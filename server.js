const express=require('express');const path=require('path');const app=express();const PORT=process.env.PORT||3000;const KEY=process.env.CRICKET_API_KEY;app.use(express.static(path.join(__dirname,'public')));async function api(ep,params={}){if(!KEY)throw Error('CRICKET_API_KEY is not configured');const q=new URLSearchParams({apikey:KEY,offset:'0',...params});const r=await fetch('https://api.cricapi.com/v1/'+ep+'?'+q);const d=await r.json();if(!r.ok)throw Error(d.reason||d.message||('API HTTP '+r.status));return d}app.get('/api/current-matches',async(_,res)=>{try{res.json(await api('currentMatches'))}catch(e){res.status(502).json({error:e.message})}});app.get('/api/match-scorecard',async(req,res)=>{try{if(!req.query.id)return res.status(400).json({error:'Missing match id'});res.json(await api('match_scorecard',{id:req.query.id}))}catch(e){res.status(502).json({error:e.message,note:'Detailed scorecard/ball-by-ball access may depend on your CricketData plan.'})}});app.listen(PORT,()=>console.log('SportBuzzLive '+PORT));
